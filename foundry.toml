[profile.default]
src = 'src'
out = 'out'
libs = ['node_modules', 'lib']

[rpc_endpoints]
ethereumSepolia = "${ETHEREUM_SEPOLIA_RPC_URL}"
milkomedaTestnet = "${MILKOMEDA_TESTNET_RPC_URL}"

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
